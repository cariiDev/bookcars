apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bookcars-backend-ingress
  namespace: bookcars
spec:
  tls:
    - hosts:
        - api-dev.carii.org
      secretName: wildcard-secret
  rules:
    - host: api-dev.carii.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bc-backend
                port:
                  number: 4002