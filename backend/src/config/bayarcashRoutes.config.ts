const routes = {
  createPaymentIntent: '/api/create-bayarcash-payment',
  checkPaymentStatus: '/api/check-bayarcash-payment/:paymentId',
  handleCallback: '/api/bayarcash-callback',
}

export default routes
